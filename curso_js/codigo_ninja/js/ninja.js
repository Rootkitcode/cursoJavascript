/**Muchos programadores ninja del pasado utilizaron estos trucos para
 * afilar la mente de los mantenedores de codigo
 */

//fragmento de codigo tomado de una libreria muy conocida
//analicemos este fragmento

i = i ? i < 0 ? Math.max(0, len + i) : i : 0;

/**podemos tambien abreviar palabras de variables o funciones que declaremos
 * ejemplo: list lts, browser brsr, etc
 */

/**otra tecnica de codigo ninja es declarar variables con letras
 * ejemlo a, b, r   etc, esto no es lo mas optimo pero en algunas librerias se
 * encuentran y podemos tardar bastante tiempo tratando de adivinar a que hace referencia
 * esta es una forma de que tu codigo solo sea digno de que algunos pocos logren entenderlo
 * puede ser una arma de doble filo ya que debes de tener muy claro que estas declarando para que luego no
 * termines sin saber que fue lo que hiciste
 */
